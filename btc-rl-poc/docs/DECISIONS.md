# DECISIONS — the ledger of what exists and why

Every create/retire/policy decision, with its evidence and who decided.
Append-only. Reconstructed 2026-08-28 from NOTES.md and git history;
live entries follow the same shape. "Human" = the project owner; every
live-money policy terminates with them (PROGRAM.md §3).

| date | decision | evidence | decided by | status |
|---|---|---|---|---|
| 08-20..24 | kb2 (market-anchored blend) is THE deliverable; kb stays control | pre-registered design | human | standing |
| 08-25 | retire kbf; retire kb6 from trading | kb6 UP-recall 63%, coverage 37%; later: calib slope 0.33 ≈ noise | human + evidence | retired |
| 08-25 | pt3 Disciplined frozen at pre-registered policy v2 (≥0.77) | pre-registration discipline | human | frozen control |
| 08-26 | Saver stake 0.25→0.10 | ~2.5× Kelly; maxDD $17.6k | human | live |
| 08-26 | Gambler v2: ≥0.77 gate + $10k reset, history preserved | gated tier 79.4% vs 65.6% ungated | human | superseded by v2.1 |
| 08-26 | pt6 MLE launched (supervised edge + half-Kelly) | industry standard vs RL data-appetite analysis | human | live |
| 08-26 | SEV-0 declared; all changes become treatments | full-graph audit | human | standing law |
| 08-28 | M7 hour policy DROPPED (not deferred) | 2 "bad hours" vs 1.98 expected by chance, p=0.60 | evidence | dead |
| 08-28 | RCA corrected: tier-1 defect is dispersion not direction | drift arms never trade; feeding path +$1.76 bias; bands 74–76% | evidence | recorded |
| 08-28 | MAE→MSE as headline error metric everywhere | user directive; MASE kept beside MSSE (fat-tail caveat recorded) | human | live |
| 08-28 | 9 bug fixes incl. trader lockout + treatment stale-quote | each verified vs the metric that exposed it; 3/3 prediction matches | evidence (bug class) | live |
| 08-28 | Gambler v2.1: edge ≥2¢ at ACTUAL fill | −1.7-pt stated-edge entry at $1,911.85; conf≡mkt degeneracy proven | human (flagged row) + evidence | live |
| 08-28 | pt7 Patient + pt8 Ideal launched; M11 twins | naive limit −5.21% paired (adverse selection) → measured, defended | human directive | live, collecting |
| 08-28 | M12 EV-ranked leader as treatment; Follower stays frozen | win-rate leaderboard seats market-echo arms 62%/day; control integrity | evidence + charter | live, flat backfill |
| 08-28 | SEV-1 logged; org charter (PROGRAM.md); this ledger created | TA directive | human | this document |

| 08-29 | Gambler v3 (D-gambler-sizing RESOLVED): 33% stake KEPT — the aggressive curriculum is the exhibit — but every settle that lifts the bankroll above the $10k start sweeps the excess to a withdrawal ledger (wd_c on the settling row, mirroring Saver's skim_c); withdrawals can never be re-staked; fresh $10k at PT4_RESET2_TS, v1/v2 history preserved. Exposure now bounded at 0.33×$10k forever | owner directive; SEV-1 finding was that sizing, not the gate, was the exposure — the sweep caps compounding risk while keeping the lesson | human | live (daemon restarted) |
| 08-29 | M1 v3 (D-m1-future RESOLVED): redesigned with shorter memory (window 150→50, warm 20, refit 3) AND kept shadow-only as a drift instrument — p_m1 is never a decision input. from_dict made config-sovereign (evidence restored, hyperparameters from code) so the retune actually applies to restored state | owner directive; introspection showed the 150-window fit lagging a miscalibration that flips within a day | human | live (daemon restarted) |

| 08-29 | Loss-review law (owner): every losing trade gets an automated RCA + a graded SEV on the six-level ladder (SEV-0 integrity-impossible row · SEV-1 own-rule breach · SEV-2 confident-wrong ≥0.77 · SEV-3 oversized >2× stated-edge Kelly · SEV-4 bad context knife/regime · SEV-5 expected variance, response only) + a coach response to the trader + a treatment-path proposal when a cause dominates. Version-aware grading: rows judged against the rule in force when made (first run produced 48 false SEV-1s judging v1 rows by today's gates — fixed before shipping). First real catch: one 08-26 window where Gambler staked 42% vs 33% cap and Saver 31% vs 25%, same minute — under investigation | owner directive; scripts/loss_review.py on the 10-min cron; 413 losses graded: 2×SEV-1, 109×SEV-2, 35×SEV-3, 131×SEV-4, 136×SEV-5 | human (law) + evidence | live |
| 08-29 | Trader ML-migration program (owner): traders evolve from rule-based to ML-based regimes, starting with bid PRICING and PACING. Method ruling: regression / full-information counterfactual models now (every candidate limit price and entry minute is scorable on logged quote paths — auditable row-by-row); deep RL deferred to multi-ticker scale (data appetite, no counterfactual audit). First artifact: fill_curve.json — fill-rate/win-given-fill/EV per limit offset δ∈[0,8]¢ + logistic fill_prob(δ, mins_left) + pacing buckets. Headline finding: win-given-fill falls monotonically 67.9%→47.0% with bid depth — adverse selection now measured, not asserted; 9–6min is the least-bad taker entry window. Graduates only as a future M13 treatment via SPRT; nothing changes live | owner directive; scripts/emit_fill_curve.py, model basis (same family as M11/t_limit) | human (program) + evidence | evidence collecting |

| 08-29 | Experiment graph unified (TA brief #3): board.html → "Experiment Program" (lifecycle+causality), metrics_lab decision board → "Experiment Analysis" (evidence) — two views of one registry (program.json). New paired incremental branch analysis on identical windows: M2 adds nothing to M8 (−3.7¢, P13%), M3 INTERFERES with M8 (−7.1¢, P6%), M10 genuinely adds to itself+M8 (+3.6¢, P95%), M11+M8's benefit is inherited from M8 (−2.9¢ vs M8 alone, P19%); cross-basis pairs refused. Governance preserved: analysis recommendations (PRIORITY/CONTINUE/HOLD/DIAGNOSE/RETIRE/REDESIGN/INVALID/PROMOTE) never change lifecycle — only the owner does. Decision inbox raised: retire M11 standalone? retire M3 branch? redesign M12? create M13 Edge Band? | TA directive; emit_program.py + diagnosis layer; all numbers from treatments.jsonl paired windows | human (directive) + evidence | shipping |

| 08-29 | Owner ratified ALL FOUR inbox items ("all 4"): (1) M11 maker-limit RETIRED standalone — mechanism contradicted by the fill curve; M11+M8 continues as a conditional-effect study only; (2) M3 Fixed-Share branch RETIRED — M3+M8 ran −7.1¢ vs M8 alone (P(inc>0)=6%): the stabilizer interfered with the regime gate; (3) M12 → REDESIGN (EV objective right, implementation losing); (4) **M13 Edge Band CREATED** — enter only when claimed edge ∈ [2¢, 12¢], band pre-registered in code (M13_MIN/MAX_EDGE_C), never fitted; floor from Gambler v2.1 evidence, ceiling from the kb5/pt6 anti-signal weights. 15 of 16 registered treatment slots used — family-wise α unchanged. Retired treatments keep accruing evidence for the record; retirement = no promotion path, not deletion | owner ("yes… all 4"), paired incremental evidence in program.json; daemon restarted with M13 live | human | live |

| 08-29 | REAL-MONEY-EQUIVALENT qualification program adopted (owner's master directive, 87 sections): REAL_MONEY_EQUIVALENT_STANDARD.yaml v1.0.0 is the immutable spec (agents may not modify; changes = governance events); emit_readiness.py machine-computes per-plane R-levels with MIN aggregation and live downgrades — no hand-typed READY anywhere. **Honest verdict v1: system R0 (security-gated), strategy NO QUALIFIED EDGE.** The single open SEV-0: production credential file `~/.kalshi_PRODUCTION_key_KEEP_OR_DELETE.pem` on owner disk — only the owner can clear it (delete/relocate); everything else assessed R1–R6 by plane with explicit blockers. Adapter scan confirms order code targets demo host only; prod API is read-only market data. Phase-5 lean cleanup executed under §5's 3-per-tier law: M2 (dominated by M8), M9 (15% coverage; idea survives in M13's floor), M2+M8 (adds nothing over M8), M1-gate (13% coverage; calibrator survives as shadow instrument) all RETIRED with evidence kept. Active: T3 = M8+M13, T4 = M10+M10+M8+M11+M8-study. Phases 1–9 tracked as tasks #31–33 | owner (master directive); readiness.json on the 10-min chain + published | human (directive) + machine (verdict) | live |

| 08-29 | D-prod-key RESOLVED (owner: "delete it"): the production Kalshi credential file removed from disk. Machine verification on the next readiness run: production_credentials_detected=false, SEV-0 count 0, security plane R0→R1, **system R0→R1** (weakest now execution+security, both with explicit blockers). Only the demo key remains; live execution stays physically limited. The key is regenerable from the owner's Kalshi account if ever deliberately wanted | owner order; readiness.json machine-verified | human | closed |

| 08-29 | Standard v1.0.2 ratified (evidence updates, running under the owner's standing "go / don't stop" directive): SRM gate live on the decision board (deficits/dupes/missing → INVALID; 13/13 clean); leakage-canary + placebo suite 6/6 PASS on the audit cron (future-label, quote-age, timestamp canaries; shuffled-label/random-policy/noise placebos — incl. the corrected non-zero shuffle null from veto cost-drag); monitor-of-monitors on an INDEPENDENT 5-min cron (chain-death detectable from outside; 11/11 HEALTHY); fail-closed runtime states in the daemon (FREEZE_NEW_ENTRIES when the wall is red or auditors silent >45m; settlement always continues; exported as runtime_state). experimentation R6; stale blocker texts corrected (credential SEV-0 resolved, independent money-math live) | Phase 1-2 of the master directive; artifacts: decision_board srm blocks, leakage_canaries.json, meta_monitors.json, online_status.runtime_state | human (standing directive) + evidence | live |

| 08-29 | Standard v1.0.3: execution plane R1→R2 — quote-age/delay sensitivity curves (Δ 0-5min) + E1/E2/E3 execution models live on the cron. Findings: champion edge SURVIVES the calibrated-conservative model E2 (+2.7¢/$1) and FAILS stress E3 (−3.8¢/$1); staleness curve rises with age (+42¢ at 5min stale) — the champion pays momentum-inflated asks, the adverse-selection signature confirmed by an independent method; sub-minute latency honestly UNAVAILABLE at per-minute granularity. System now R1 gated by security alone (dependency lock/SBOM) | scripts/emit_exec_sensitivity.py; spot-check exact; deterministic across runs | evidence + standing directive | live |

| 08-29 | M14 "The Oracle Call" created (owner spec: 80-90% certainty, 2-9 min into the window): pre-registered selective-prediction product — kb2 calls only at claimed confidence ≥0.80 within mins_left∈[6,13], NO CALL otherwise; promise metric = selective accuracy ≥0.80 with Wilson CI + coverage, prequential from pre-settle-stamped rows; kb5 logged as shadow caller, never merged. Launch measurement: 11 calls / 169 windows — selective accuracy 90.9% (CI [62%, 98%]), coverage 6.5%, promise MET. Coverage-raising research (calibration variants, envelope timing, pooling) proceeds only as further treatments | owner directive; exploratory table showed the 0.8-0.9 claim band verifying 79-91% across arms; scripts/emit_oracle_calls.py on the 10-min chain | human (spec) + evidence | live, collecting |

| 08-29 | LEAN MACHINE ratified (owner: "1 treatment + 1 control per layer, selection on stability/precision/recall/accuracy/MSE/losses; keep all horizons; same up/downstream; executive calls delegated"): T1 per horizon — control h-family vs treatment by val-MSE (h1↔t9-h1, h5↔t10-h5, h15↔t7-h15, h30↔t9-h30); all other T1 arms FROZEN via LEAN_RETRAIN (serve, never retrain). T2 early-caller product — CONTROL kb2 (best Brier 0.2166, log-loss 0.623, stability 2.0%, selective 88%@0.75) vs TREATMENT kb9 (selective 83%@0.75; kb5 disqualified — no early rows; kb8's high-conf claims verify at only 67%); tracked in oracle_calls.early_ct on ML fundamentals. Product line: M14 v1 (6.5% cov, 90% acc) · v2 kb5@0.72 mid-window (targeting ~47% cov) · early kb2/kb9@0.75 first-2-3-min (targeting ~22% cov) — all prequential from registration. M2-M13 racing zoo FROZEN, evidence kept. Where-we're-wrong answer recorded: 80-90% on EVERY window 2-3 min in is physically impossible (the market itself is 71% at its own ≥0.65 claims there); 80-90% selective at ~1-in-5 coverage is real and now productized; coverage growth is the research axis | owner directives + measured scorecards this session | human (directive) + machine (selection) | live (daemon restarted, lean) |

| 08-29 | **ALPHA CAPTURE ENGINE adopted** (TA architecture v2, dry-run-tested): the decision problem is CROSS/POST/WAIT/PASS on a continuously updated terminal probability — not UP/DOWN; prediction and execution never share an objective. Five jobs × ≤2 arms (σ-structural + residual-tabular; compact micro model; value-of-wait GBDT; cross-ask vs edge-aware-limit; empirical vs conformal calibration); three alpha families only (terminal geometry / cross-market repricing / overreaction); Robust Edge = p_LCB − ask − fees − slippage − toxicity as the central quantity; alpha ledger Available→Captured→Realized with ECR/ESR/TAE; North Star = LCB net alpha per eligible window; **incumbent frozen as immutable control — v2 may not claim success until it beats it prequentially after costs (§34)**. P0 executed same-hour: Layer-A event capture LIVE (scripts/event_capture.py — Coinbase WS trades+L1 with venue timestamps, Kalshi 1s quote polls, 3-stamp schema, hour-sharded events/, watchdog + meta-monitor row). Deferred per the architecture's own rules: L2 depth (until lead-lag half-life justifies), sequence challenger (until simple system has evidence), RL (indefinitely), LLM in decision path (never) | TA proposal + owner standing directive; first shard flowing (~4 ev/s, 0 errors, sub-ms persist) | human (architecture) + evidence | live — data accruing |

| 08-29 | **M1 CLOSED — Forward Experiment Platform COMPLETE** (PM ruling): A3 registers, watches, depth-gates, triggers, pairs, marks out, attributes, audits, fails closed without intervention; first eligible window settled (paired Δ +16.7¢/$1, ECR 0.80, NO-side depth-gated fill; n=1, verdict COLLECTING, watch-flags informational until n≥10). **M2 OPENED — ML & Research Observatory** under the six-workstream program (task #35): P0 METRIC_DICTIONARY.yaml v1.0.0 shipped as THE shared metric contract (formula/unit/statistical-unit/denominator/provenance/phase/CI/freshness/owner/gate per metric; OFFLINE/SHADOW/FORWARD never merge; definition changes = governance events). Then: offline walk-forward evaluator, immutable training-run registry (promotion-gap = does offline predict forward), online model health + parity invariant, the five-agent org (structured outputs, no promotion authority), five-page site (Desk/Performance/Models/Research/Firm). Explicitly STOPPED: model-zoo expansion, RL, new LSTMs/TimesFM work, feature proliferation, more traders, extra agents, dashboard sprawl, manual threshold tuning. Two clocks run in parallel: market clock (A3 untouched) and engineering clock (M2) | PM directive + first-window evidence | human (PM) + machine | live |

Proposal queue (PORTFOLIO team writes here; human ratifies):
- **PROPOSE: retire or redesign M1 (Platt calibration layer).**
  Evidence (introspection wave, 08-29 snapshot): prequential log-loss
  is WORSE calibrated than raw for ALL NINE arms (+0.034..+0.164, kb7
  worst) — the layer's own pre-registered success criterion is met by
  nobody, and the fitted direction has flipped since 08-28 (b>1 now vs
  b<1 then): the miscalibration drifts faster than the fit. Options:
  (a) retire M1, keep raw p_up; (b) redesign with shorter memory;
  (c) keep shadow-only as a drift instrument, never as an input.
  Recommendation: (c) — it costs nothing and measures drift.
- **PROPOSE: investigate the edge-anti-signal.** kb5's claimed_edge
  weight −0.096 and pt6's conf_minus_ask −0.059: both edge-gated
  models' own regressions score "stated edge" as predicting LOSSES
  (adverse selection internalized — when you think you know better
  than the price, you usually don't). If it replicates on more data,
  the gates should invert from "edge floor" to "edge band" (too MUCH
  claimed edge is also a red flag).
- (nearest launch candidate: M10+M8 — boundaries now family-wise
  corrected to 5.66, so the bar is properly higher)

| 08-29 | Homepage rebuilt as the 7-scene narrative instrument (Prediction→Decision→Outcome→Evidence→Falsification); classic page preserved at home_classic.html as the audit-depth view | owner's design brief: "live scientific instrument, not a dashboard"; accuracy-is-not-edge as the central visual; normalized returns since policy start (start capitals shown — raw $ comparison was scientifically messy) | human (design brief) | shipping |
| 08-29 | TA metrics review adopted: a DECISION layer now sits above the registry — scripts/emit_decision_board.py computes, per treatment, bootstrap+normal 95% CI, P(Δ>0), P(Δ>edge), MDE and power at the family-wise α, paired-window concentration (top-3 share), evaluability vs activity overlap, veto decomposition (losses avoided / wins forgone on skipped windows), regime/leader/hour effect slices with worst-powered-slice and sign consistency, projected windows to an SPRT verdict, and an automatic PROMOTE / KEEP_TESTING / HOLD / KILL / INVALID state under rules pre-registered in that file. Every number derives from the daemon's own per-window paired log (treatments.jsonl); the SPRT stays the promotion authority — the state machine cannot promote anything the SPRT hasn't passed | TA review (28-section critique); first run: 10 KEEP_TESTING, 2 HOLD (t_cal/t_cheap coverage <25% — "a different product"), 0 KILL; power for the 2¢ pre-registered edge at n=168 is ~2% at α=0.003125, which is precisely why sequential testing, not fixed-horizon, is the desk's law | human (TA directive) + evidence | live, cron 10-min |
| 08-29 | Quant Universe wave 1 shipped: universe.html (explorable world atlas, one geometry × Explore/Engineer lenses), clock.html (every cadence with its proving artifact + dissected retrain + honest cost ledger), agents.html (authority cards + activity stream, agent-quality rows say "not yet metered"), museum.html (six frozen failure exhibits); world.json emitter grounds all of it in real artifacts; docs/OS_BLUEPRINT.md adopted as the engineering constitution (seven planes, four loops, SEV-0..3 taxonomy) | owner's two-worlds brief + TA "Quant Research OS" review; all pages screenshot-verified in both lenses | human (design briefs) | shipping |
| 08-29 | Skip-as-decision formalized: a treatment's stand-down is a COMPLETE scored observation (EV 0 by pre-registration), never "missing data" — pair completeness measures evaluability only, and activity overlap is reported separately without invalidating | first emitter draft wrongly marked all 12 challengers INVALID by counting deliberate skips as missing pairs; the distinction is now in code and on the board | evidence (bug class) | recorded |

| 08-29 | **GREAT SIMPLIFICATION ordered (PM directive) — retirement migration manifest adopted** (docs/RETIREMENT_MANIFEST.md): hard caps MAX 5 trader slots (one deliberately EMPTY — a candidate earns it), 1 control + ≤1 treatment per layer, shadows observe but never count; lifecycle machine CONTROL/TREATMENT/SHADOW/QUALIFIED/RETIRED/ARCHIVED kept separate from analysis recommendations. Roster: KEEP Follower (control), Disciplined (core thesis), MLE (shadow until justified), A3/T10 (primary forward experiment); RETIRE pt2 Ladder (redundant control-twin), ARCHIVE pt4 Gambler (stress exhibit; sizing → risk engine), CONSOLIDATE pt5 Saver (sizing → risk engine), pt7 Patient → offline naive-maker toxicity benchmark, pt8 Ideal → freeze + decompose (6 simultaneous causal changes, unattributable). ONE legacy experiment remains: M10 (control) vs M10+M8 (treatment); M11+M8 diagnostic-shadow, M13 shadow/hold; all other M-branches stop consuming runtime. Models: kb2 vs kb9 serving pair stays; kb/kb3-kb8/kbf → offline benchmark; "max 3 serving models" end-state flagged as conflicting with the standing keep-all-horizons order — awaits explicit owner call. Champion/challenger chain law: winner becomes control, loser retires with full record; no strategy forests. Execution staged TX-A manifest ✔ / TX-B runtime / TX-C UI / TX-D models | PM directive (retirement criteria A–G recorded); retirement = removal from runtime+headline only, history preserved forever | human (PM) | TX-A done, TX-B..D executing |

| 08-30 | **MASTER BUILD CONTRACT adopted** (60-section execution contract → docs/MIGRATION_MANIFEST.md): the product becomes TWO surfaces — Home (public MVP: Thesis→Price→Action, principal experiment, labeled claims) and Backend (one console: Overview/Models/Experiments/Data/Operations/Agents/Registry), superseding the 5-page IA; every page/script/service mapped KEEP/CONSOLIDATE/SHADOW/RETIRE/ARCHIVE; build order M2 observatory (metrics v2, immutable training registry, four-era lifecycle, walk-forward evaluator w/ falsification battery, parity, generalization gap) → M4 backend shell + Home last → M5 five structured agents → M6 self-healing loop (DETECT→…→RECORD; forbidden-repair list enforced by invariant); M7 blocked on the frozen §46 decision tree. A3-v1.1 stays frozen; leanness caps (5 traders/3 model roles/1 treatment) now contract law | PM master prompt; retirement TX-B already live | human (PM) | manifest committed, M2 next |

| 08-30 | **PM 126-section Migration Manifest adopted + M2/M3 Wave-1 EXECUTED**: config/METRICS.yaml v1.1.0 (canonical metric contract — windows LIFETIME/SINCE_LAUNCH/LAST_100/50/25, min-N display 10/compare 25/gate 50, trend states, universal metric row, no-private-formulas law); config/COMPONENT_REGISTRY.yaml v1.0.0 (machine-readable §85 manifest: every trader/model/experiment/service with lifecycle+runtime flags, budgets max 5 traders/3 model roles/1 treatment); results/training_runs.jsonl (immutable registry, 4,848 candidate runs derived from metrics_history; idempotent append); results/model_lifecycle.json (kb2/kb9 four-era blocks OFFLINE=NOT_RECORDED-stated-honestly/LAUNCH/CURRENT/LIFETIME + windowed metrics + trend classification refusing below compare-N; T1 serving-pair keep-rates); results/pm_snapshot.json (§111 one-screen daily state) — both on the 10-min audit chain + published. M11+M8 (t_limit_reg) moved SHADOW→DIAGNOSTIC ARCHIVE per §41 (runtime stopped, grace window in invariant); decision board now 6 rows. Current truth: A3 n=3 Δ +3.2¢ (reverted from n=1 +16.7¢ — the evidence-first UI is why we don't celebrate at n=1); kb9 lifetime BSS −0.044 (nobody beats the book), trend IMPROVING (last-25 Brier 0.181 vs prior-75 0.221) | PM master contract + measured artifacts | human (PM) + machine | Wave 1 done; Wave 2 = offline/walk-forward evaluator + parity |

| 08-30 | **M2 Wave 2 shipped — the ONE evaluation engine** (scripts/eval_engine.py v1.0.0 → results/model_offline.json, on the audit chain): chronological 5-fold walk-forward over prequential decision-time series, per-fold Brier/log-loss/BSS/ECE/calibration-IRLS/selective-layers(.60-.90)/economic-replay(frozen taker, τ=.75), fold discipline median/worst/variance/fraction-beating-market (one good fold qualifies nothing), BLOCKING falsification battery (timestamp order, duplicate windows, outcome-leak canary, label shuffle, prediction shuffle, random strategy; PIT reconstruction = declared NOT_AVAILABLE gap, mandatory for NATIVE candidates). Battery shakeout caught its own first design flaw: Brier-based label-shuffle flagged kbf EVAL_INVALID because a hedged predictor mechanically beats a confident baseline on random labels — replaced with rank-AUC association ([0.40,0.60]); 10/10 arms then PASS. Verdict: NO arm has positive median-fold BSS (kb2 −0.0001 ≈ market as expected; kb9 −0.020, beats-mkt 2/5 folds) — "nobody beats the book" now has fold-level walk-forward evidence. Also: training_runs.jsonl regenerated with registry_origin=RECOVERED_HISTORY provenance (PM correction — 4,848 rows are reconstructed, only training-time emissions may claim NATIVE); pm_snapshot.json gained changed_since_last_snapshot diff + next_unblocked_action; board.html A3 card shows the estimate path (n=1 +16.7¢ → …) + best-window contribution; model_online.json rolling-window artifact. Market clock: A3 n=6, Δ −18.6¢ — 2 fills both beat control (+20.5/+16.7¢) but 4 straight MISSED winners (miss-cost mode, pre-registered failure watch, arms at n≥10). NO ACTION — COLLECTING | PM Wave-2 directive + engine output | human (PM) + machine | live; parity (M2.5) = next, needs feature-snapshot store |

| 08-30 | **M2.5 SHIPPED — PIT snapshot store + live↔replay parity + qualification object + A3 canonical decomposition** (PM directive): (1) online.py _pit_snapshot persists at every serving-pair inference (kb2 CONTROL blend inputs p_cal/k_pup/bw; kb9 TREATMENT the exact TimesFM close series+strike+horizon) with prediction_id, 4 timestamps, feature hash, model family — "why did kb9 output 0.785?" is now answerable forever; (2) scripts/check_parity.py: kb2 exact recompute 100% every audit run, kb9 sampled replay via --sample (deferred is declared, never PASS); FIRST LIVE RESULT: 2/2 PASS incl. a real TimesFM replay — the frozen model is deterministic on stored inputs; model-parity invariant added (15/15 green), FAIL = promotion freeze + red wall; (3) model_qualification.json: one verdict per candidate (INVALID/OFFLINE_FAIL/SHADOW_ELIGIBLE/FORWARD_CANDIDATE) — all 10 arms OFFLINE_FAIL as expected (no positive median-fold BSS anywhere); (4) emit_a3.py decomposition (measurement-only): gross_fill_gain +37.3¢ − timeout_control_pnl 149.1¢ = net_waiting_value −111.8¢, identity_residual 0.000000, per-window econ classes (2×A_FILLED_GOOD, 4×C_MISSED_WINNER) — the canonical economic explanation of A3; (5) self_trend vs baseline_status split in model artifacts (kb9: self-trend IMPROVING · baseline NOT_ESTABLISHED — never one badge) | PM Wave-2 verdict + live parity evidence | human (PM) + machine | M2.5 green; M3+M4 unblocked per PM sequence |
